# Set this to the FastAPI origin when the backend is available.
# Leave empty to use the local sessionStorage prototype adapter.
NEXT_PUBLIC_API_BASE_URL=

# Required to access the owner-only /admin dashboard.
ADMIN_DASHBOARD_PASSWORD=replace-with-a-strong-owner-password
